node_modules/
lib/
*.tsbuildinfo
coverage/
.DS_Store
*.tgz

# Assembled by packages/cli/create's prepack; the sources live in templates/ and docs/
packages/cli/create/template/
packages/cli/create/docs/

# Scaffold smoke-test output
.smoke/

# The dsh source snapshot and the code graphs over it: fetched and rebuilt by
# `pnpm run dsh:graph` from one immutable tag, so the recipe is the artifact.
.dsh-source/
.codegraph/
